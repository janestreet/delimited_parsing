(library (
  (name delimited)
  (public_name delimited_parsing)
  (libraries (
    async
    core
    core_extended.delimited_kernel
    expect_test_helpers
    ))
  (preprocess (pps (ppx_jane ppxlib.runner)))
  ))


(jbuild_version 1)
